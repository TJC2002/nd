<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.nd.mapper.FileMapper">
    
    <select id="getFileById" resultType="com.example.nd.model.File">
        SELECT * FROM files WHERE id = #{id} AND is_deleted = 0
    </select>
    
    <select id="getFilesByFolderId" resultType="com.example.nd.model.File">
        SELECT * FROM files WHERE parent_folder_id = #{folderId} AND is_deleted = 0 ORDER BY created_at DESC
    </select>
    
    <select id="getFilesByUserId" resultType="com.example.nd.model.File">
        SELECT * FROM files WHERE user_id = #{userId} AND is_deleted = 0 ORDER BY created_at DESC
    </select>
    
    <insert id="insertFile" parameterType="com.example.nd.model.File" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO files (user_id, parent_folder_id, file_name, original_name, file_size, mime_type, file_hash, storage_path, storage_type, version, is_deleted, created_at, updated_at)
        VALUES (#{userId}, #{parentFolderId}, #{fileName}, #{originalName}, #{fileSize}, #{mimeType}, #{fileHash}, #{storagePath}, #{storageType}, #{version}, 0, NOW(), NOW())
    </insert>
    
    <update id="updateFile" parameterType="com.example.nd.model.File">
        UPDATE files
        SET parent_folder_id = #{parentFolderId}, file_name = #{fileName}, original_name = #{originalName}, file_size = #{fileSize}, mime_type = #{mimeType}, storage_path = #{storagePath}, version = #{version}, updated_at = NOW()
        WHERE id = #{id}
    </update>
    
    <update id="deleteFile">
        UPDATE files SET is_deleted = 1, updated_at = NOW() WHERE id = #{fileId}
    </update>
    
    <select id="getFileByHash" resultType="com.example.nd.model.File">
        SELECT * FROM files WHERE file_hash = #{fileHash} AND is_deleted = 0 LIMIT 1
    </select>
    
    <update id="updateFileVersion">
        UPDATE files SET version = version + 1, updated_at = NOW() WHERE id = #{fileId}
    </update>
</mapper>